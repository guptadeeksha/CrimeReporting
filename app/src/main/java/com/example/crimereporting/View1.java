package com.example.crimereporting;import androidx.annotation.NonNull;import androidx.annotation.Nullable;import androidx.appcompat.app.AppCompatActivity;import android.app.Activity;import android.content.Context;import android.content.Intent;import android.content.SharedPreferences;import android.database.Cursor;import android.database.sqlite.SQLiteDatabase;import android.os.Bundle;import android.view.View;import android.view.ViewGroup;import android.widget.ArrayAdapter;import android.widget.Button;import android.widget.EditText;import android.widget.ListView;import android.widget.SimpleCursorAdapter;import android.widget.TextView;import android.widget.Toast;import android.app.ListActivity;import java.util.ArrayList;import java.util.List;public class View1 extends ArrayAdapter<ViewComplaints> {    public View1(Activity context, int resource, ArrayList<ViewComplaints> objects) {        super(context, 0, objects);    }    @NonNull    @Override    public View getView(int position, @Nullable View convertView, @NonNull ViewGroup parent) {        ViewComplaints vc = getItem(position);        View list_view =convertView;        TextView id =(TextView)list_view.findViewById(R.id.ID);        id.setText(vc.getID());        TextView em =(TextView)list_view.findViewById(R.id.e_mail);        id.setText(vc.getemail());        TextView pol =(TextView)list_view.findViewById(R.id.pol);        id.setText(vc.getpolice());        TextView vic =(TextView)list_view.findViewById(R.id.vic);        id.setText(vc.getvictim());        return list_view;    }}